$('#secchi_depths').html("<%= j (render @secchi_depths) %>");

<% flash.each do |type, message| %>
  $("#flash_messages").html("<%= type.to_s.humanize %>: <%= message.html_safe %>")
  $("#flash_messages").attr("id","notice")
  $("#notice").show().hide(2000)
 
<% end %>